const config = {
  plugins: {
    "@tailwindcss/postcss": {
      taiwindCss: {
        theme: {
          colors: {
            // Override with traditional rgb colors
          }
        }
      },
    },
  },
};

export default config;
